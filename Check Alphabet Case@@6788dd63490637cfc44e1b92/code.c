#include <stdio.h>
int main()
{
    char alphabet;
    scanf("%c",&alphabet);
    if (isupper(alphabet))
    {
        printf("Uppercase"
        )
    }
    else{
        printf("Lowercase");
    }
    return 0;
}