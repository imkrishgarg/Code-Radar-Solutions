#include <stdio.h>
int main()
{
    char alphabet;
    scanf("%c",&alphabet);
    if (isupper(alphabet))
    {
        printf("Uppercase"
        )
    }
    else if(islower(alphabet))
    {
        printf("Lowercase");
    }
    return 0;
}