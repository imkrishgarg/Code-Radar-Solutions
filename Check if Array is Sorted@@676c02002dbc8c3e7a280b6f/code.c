#include<stdio.h>
int main()
{
    int n,fir_ele;
    scanf("%d",&n0;)
    int arr[n],i;
    for(i=0;i<n;i++)
    {
        scanf("%d",a[i]);
    }
    fir_ele=a[0];
    for(i=0;i<n;i++)
    {
        if(fir_elemt>a[0])
        {
            printf("Not Sorted");
        }
        else
        {
            printf("Sorted");
        }

    }
    return 0;
}