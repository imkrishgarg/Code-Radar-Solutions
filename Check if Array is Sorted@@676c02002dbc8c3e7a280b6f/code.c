#include<stdio.h>
int main()
{
    int n,fir_ele;
    scanf("%d",&n);
    int arr[n],i;
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    fir_ele=arr[0];
    for(i=0;i<n;i++)
    {
        if(fir_ele>arr[i] && fir_element==a[0])
        {
            printf("Not Sorted");
            break;
            
        }
        else
        {
            printf("Sorted");
            break;
            
        }

    }
    return 0;
}